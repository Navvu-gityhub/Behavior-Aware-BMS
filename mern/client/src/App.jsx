import { BeaconDashboard } from './beacon/BeaconDashboard.jsx';

/**
 * BEACON.
 *
 * One surface. The earlier "Live Fleet" shell, its tab row and its parallel
 * panels are gone: two dashboards meant two brand marks and two provenance
 * banners disagreeing about the same run.
 */
export default function App() {
  return <BeaconDashboard />;
}

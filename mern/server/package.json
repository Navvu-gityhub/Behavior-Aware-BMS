{
  "name": "beacon-gateway",
  "version": "0.1.0",
  "description": "Thin Express gateway in front of the existing Python/FastAPI pipeline. Does not reimplement any pipeline logic.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test src/__tests__/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}

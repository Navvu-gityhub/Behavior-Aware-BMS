-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0

# Linting and type checking. Configured in pyproject.toml; `make check` runs
# the same commands CI does, so a green local check means a green pipeline.
ruff>=0.6.0
mypy>=1.11.0
pandas-stubs>=2.0.0
types-PyYAML>=6.0.0

# cantools is already pulled in by requirements.txt above -- src/bms/telemetry
# imports it at module level, so it is a core dependency, not an extra. It is
# re-listed here only to pin it for CI.
#
# If you install a subset that omits it, tests/test_can_dbc.py and
# tests/test_can_vehicle_registry.py skip cleanly (pytest.importorskip) instead
# of failing collection and aborting the whole run. The `no-optional-extras`
# CI job installs without it specifically to keep that claim true.
cantools>=4.4.0
